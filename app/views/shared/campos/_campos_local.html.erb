<% if current_user.unidade_id == 53 %>
  <%= collection_select define_model(encontra_model(request.request_uri ).to_s.singularize), :localizacao_id ,@localizacoes,:id,:concat_locale, :include_blank => "Selecione" %>
<%else%>
  <%= collection_select define_model(encontra_model(request.request_uri ).to_s.singularize), :localizacao_id ,@localizacoes,:id,:local_guardado, :include_blank => "Selecione" %>
<%end%>

